{
  "name": "collabhub-e2e",
  "private": true,
  "scripts": {
    "test:e2e": "playwright test --project=chromium --config=playwright.config.ts",
    "test:e2e:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "wait-on": "^7.0.1"
  }
}
